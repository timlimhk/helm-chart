apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-tim-ecp-flask.fullname" . }}
  labels:
    {{- include "helm-tim-ecp-flask.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-tim-ecp-flask.selectorLabels" . | nindent 4 }}
