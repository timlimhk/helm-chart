apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-tim-ecp-flask.fullname" . }}-test-connection"
  labels:
    {{- include "helm-tim-ecp-flask.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-tim-ecp-flask.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
